rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ─── Prescriptions: Public read, Admin upload ───
    match /prescriptions/{fileName} {
      allow read: if true; // Let patients download their PDF prescriptions
      allow write: if request.auth != null; // Admin authenticated users only
    }
  }
}
